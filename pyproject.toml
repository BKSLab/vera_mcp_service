[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]  # длина строки — много кириллических docstring-комментариев, не критично

[tool.ruff.lint.isort]
known-first-party = ["app"]
